// Cloudinary configuration
const cloudinaryConfig = {
  cloudName: 'dn2zaiwhs', // Your Cloudinary cloud name
  uploadPreset: 'train-ticket-admin', // Create an unsigned upload preset in your Cloudinary dashboard
  apiKey: '', // Optional: only needed for signed uploads (we're using unsigned uploads)
};

export default cloudinaryConfig;
